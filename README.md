# password-strength-checker
Password Strength Checker is a simple Python project that evaluates how strong a password is based on multiple security conditions — such as length, uppercase and lowercase letters, numbers, and special symbols. It helps users create stronger passwords by giving a clear strength rating (Weak, Moderate, or Strong) and a detailed feedback report.


# 🔐 Password Strength Checker

A simple yet effective **Python project** that checks the strength of a password based on several security parameters such as **length**, **uppercase and lowercase letters**, **numbers**, and **symbols**.  
It provides users with feedback to help create strong and secure passwords.

---

## 🧠 Features

- ✅ Checks **minimum length** (8 characters)
- ✅ Detects **uppercase**, **lowercase**, **numbers**, and **symbols**
- ✅ Displays detailed feedback for each condition
- ✅ Rates password as **Weak ⚠️**, **Moderate 😐**, or **Strong 💪**
- ✅ Uses **only built-in Python libraries**
- 🔒 Optional hidden password input for privacy

---

## 🧩 Technologies Used

- **Language:** Python  
- **Libraries:**  
  - `re` – for regular expressions  
  - `string` – for character sets  
  - `getpass` – for hidden password input (optional)  
- **Optional:**  
  - `tkinter` – for GUI interface (if added later)  
  - `colorama` – for colored terminal output

---

## ⚙️ Installation

1. **Clone this repository**
   ```bash
   git clone https://github.com/yourusername/password-strength-checker.git

